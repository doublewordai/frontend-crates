# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

[package]
name = "dynamo-parsers"
readme = "README.md"
version = "7.0.1"
edition.workspace = true
description = "Reasoning and tool-calling parsers for OpenAI-compatible inference output."
authors.workspace = true
license = "Apache-2.0"
homepage.workspace = true
repository.workspace = true
keywords = ["llm", "parser", "tool-calling", "reasoning", "inference"]
include = ["src/**/*", "Cargo.toml", "README.md"]

[dependencies]
anyhow.workspace = true
serde = { workspace = true, features = ["derive", "rc"] }
serde_json = { workspace = true, features = ["raw_value"] }
tokio = { workspace = true, features = ["full"] }
tracing.workspace = true
uuid = { workspace = true, features = ["v4", "serde"] }

regex.workspace = true
openai-harmony.workspace = true
ruff-python-ast.workspace = true
ruff-python-parser.workspace = true
num-traits.workspace = true

# Streaming tool-call jail (ported from dynamo lib/llm). The jail wraps the
# streaming parsers, so it needs the OpenAI chat stream protocol types plus the
# async-stream/futures combinators. dynamo-protocols has no dependencies, so
# dynamo-parsers -> dynamo-protocols stays acyclic.
# Registry requirement, as in the published manifest: a git consumer otherwise gets this
# repository's own dynamo-protocols next to the registry one its other crates use.
dynamo-protocols = "5.0.1"
async-stream.workspace = true
futures.workspace = true
aho-corasick.workspace = true

[dev-dependencies]
rstest.workspace = true
